import { FC } from 'react';

import Typography from '@material-ui/core/Typography';

const Home: FC = () => {
  return (
    <div>
      <Typography variant="h4">Hello HomePage!</Typography>
    </div>
  );
};

export { Home };
